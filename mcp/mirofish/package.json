{
  "name": "opencode-mirofish-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP server exposing the MiroFish prediction engine REST API as typed tools (health, projects, ontology, graph, simulation, report).",
  "main": "index.js",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
